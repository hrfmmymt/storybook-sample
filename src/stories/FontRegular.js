import React from 'react'
import FontRegular from '../components/FontRegular'

const FontStory = ({children}) => (
  <div style={{margin: '30px'}}>
    <FontRegular children={children} />
  </div>
)

export default FontStory
