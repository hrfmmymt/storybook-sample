import React from 'react'
import Heading from '../components/Heading'

const HeadingStory = ({children}) => (
  <div style={{margin: '30px'}}>
    <Heading children={children} />
  </div>
)

export default HeadingStory
