import React from 'react'
import Font from '../components/Font'

const FontStory = ({children}) => (
  <div style={{margin: '30px'}}>
    <Font children={children} />
  </div>
)

export default FontStory
